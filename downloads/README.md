# downloads

Patch files (`.zg3`, `.zg3x`, `.zg5`, etc.) downloaded from the Tonelib forum are saved here.

To download a patch, open the TUI browser and press `d` on any listed patch:

```bash
.venv/bin/python main.py browse
```
